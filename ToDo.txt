dev environment

Change made on branch V 1.0.0

httpInterceptorService - add sessionId
global error handler

put entire model onto $scope rather than assiging properties

remove login from history
on next\prev - clear current page
edit accounts - unique values
cancel on result edit if come from somewhere
admin to see competitors results
competitor - what are they seeing in my results - not every workout
download my results
date picker
back button when navigating from my results to results
toolbar styling

admin:
backup database** can't do this remotely
ftp backup to dropbox folder**
delete old backup files from remote folder
create spreadsheet
create google docs spreadsheet
delete sessions




set nocount on
select 'drop table ' + s.name + '.' + t.name from sys.tables t join sys.schemas s on t.schema_id = s.schema_id
select 'drop procedure ' + s.name + '.' + t.name from sys.procedures t join sys.schemas s on t.schema_id = s.schema_id
